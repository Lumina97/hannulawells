import React from "react";
import About from "./About";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="!scroll-smooth">
      <About />
    </div>
  );
};

export default page;
