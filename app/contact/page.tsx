import React from "react";
import Contact from "./Contact";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="!scroll-smooth">
      <Contact />
    </div>
  );
};

export default page;
