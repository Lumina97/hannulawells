import React from "react";
import Content from "./Content";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="!scroll-smooth">
      <Content />
    </div>
  );
};

export default page;
